<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kapilyadav.medicine">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Activity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.AddMember" />
        <activity
            android:name=".Activity.DiseaseActivity"
            android:label="@string/title_activity_disease"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Activity.AddDisease" />
        <activity android:name=".Activity.AboutDeveloper" />
        <activity
            android:name=".Activity.MedicineActivity"
            android:label="@string/title_activity_medicine_list"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Activity.AddMedicine"></activity>
    </application>

</manifest>